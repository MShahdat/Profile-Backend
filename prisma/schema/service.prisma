model Service {
  id          String  @id @default(uuid())
  
  icon        String
  iconColor   String
  bgColor     String
  title       String
  description String
  order       Int     @default(0)
  isPublished Boolean @default(true)

  updatedAt DateTime @updatedAt
}
