model Hero {
  id Int @id @default(1)

  greeting    String?
  name        String
  role        String
  titleLine   String[]
  description String
  passion     String
  isPublished Boolean @default(true)

  updatedAt DateTime @updatedAt

  @@map("hero")
}
