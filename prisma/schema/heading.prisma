model Heading {
  id          Int     @id @default(autoincrement())

  name        String  @unique
  icon        String?
  title       String?
  description String?
  isPublished Boolean @default(true)

  updatedAt DateTime @updatedAt

  @@map("heading")
}
