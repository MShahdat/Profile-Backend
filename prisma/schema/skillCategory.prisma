model SkillCategory {
  id    Int     @id @default(autoincrement())

  type  String
  icon  String?
  order Int     @default(0)
  isPublished Boolean @default(true)

  updatedAt DateTime @updatedAt

  skill Skill[]
}
